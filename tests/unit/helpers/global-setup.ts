export function setup() {
    console.log(
        '[AI-CONTEXT] Testing rules: .claude/rules/testing.md — use timeline DB with --db <path>, never re-run tests without making changes in between, investigate one failure at a time'
    );
}
